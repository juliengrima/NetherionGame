{% extends 'base.html.twig' %}

{% block title %}WebGLDemo{% endblock %}

{% block body %}
    <h1>WebGLDemo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ web_g_l_demo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ web_g_l_demo.name }}</td>
            </tr>
            <tr>
                <th>Lin</th>
                <td>{{ web_g_l_demo.lin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_web_g_l_demo_index') }}">back to list</a>

    <a href="{{ path('app_web_g_l_demo_edit', {'id': web_g_l_demo.id}) }}">edit</a>

    {{ include('web_gl_demo/_delete_form.html.twig') }}
{% endblock %}
