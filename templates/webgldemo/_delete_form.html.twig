<form method="post" action="{{ path('app_web_g_l_demo_delete', {'id': web_g_l_demo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ web_g_l_demo.id) }}">
    <button class="btn">Delete</button>
</form>
